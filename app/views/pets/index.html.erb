<h1>All Pets for <%= @owner.email %></h1>
<hr>
<% @owner.pets.each do |pet| %>
  <p>
    <strong><%= link_to pet.name, owner_pets_path(@owner, pet) %></strong>
    (<%= pet.breed %>)
  </p>
<% end %>
